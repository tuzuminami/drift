{
  "name": "@tuzuminami/drift",
  "version": "0.1.0",
  "description": "Scenario and context orchestration primitives for deterministic conversational systems.",
  "type": "module",
  "license": "Apache-2.0",
  "private": false,
  "files": [
    "dist/src",
    "migrations",
    "openapi",
    "schemas",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "db:migrate": "npm run build && node dist/src/postgres.js migrate",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run build && npm run test && npm run check:contracts && npm run check:migrations && npm run check:private-boundary",
    "check:contracts": "node scripts/check-public-contracts.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "check:private-boundary": "node scripts/check-private-boundary.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
